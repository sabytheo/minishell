Parsing :



Exec :
Avant toute commande -> parcourir la liste (c'est surement du parsing)
	Check si on trouve < ou > ou | ou << ou >>
	Check son type
		Si type 0, alors c'est une string et on peut copier
		Si != 0, changer le std_fileno en fonction ?
		comme ca on peut putstr la dedans ??


